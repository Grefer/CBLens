# 版本说明

## 未发布

Windows 自动查找优先定位本机 Wind 安装目录，再查找其中的 `x64` 等接口目录；支持从系统安装记录定位非默认盘的 Wind。独立 Python 的 `site-packages` / `WindPy.pth` 作为补充查找来源。无需运行外部 Python，也不会执行 `.pth` 中的代码；显式设置的接口路径仍然优先。

桌面包启动明显加快。此前 PyInstaller 每次启动都换一个临时的 Matplotlib 配置目录，字体缓存因而每次重建（实测冷 8.23 秒、热 0.005 秒），而这一步发生在主窗口出现之前。现在改用按版本区分的持久缓存目录（Windows 在 `LOCALAPPDATA`、macOS 在 `~/Library/Caches`），只有首次启动和升级后各付一次代价；缓存目录不可写时保持原有行为。可用 `CBLENS_MPLCONFIGDIR` 指定位置。

界面另有两处取数改进：全库准入扫描按转债代码建索引（实测 0.81 秒 → 0.19 秒，筛选结果逐只不变），策略页的条款修补/事件预检按文件指纹缓存（在自选代码框里输入不再每敲一个字就重解析两份 JSON，实测 1.34 秒 → 0.14 秒）。两者都会在文件变化后重新读取。

策略回测的默认便宜度口径从绝对改为横截面，**这是默认选债行为变更**。此前 GUI 的「估值偏差」模板把「偏差上限」写死成 `0`（只买市价不高于理论价的），CLI 不传 `--max-deviation` 时也静默等于同一个值。而全市场中位偏差长期为正（历史基线 24 期全部在 +0.41% ~ +21.64% 之间），所以这条闸的松紧完全取决于当时的市场贵贱：实测 311 只主池上它只留下 3 只候选，Top 10 的十个仓位有七个是现金，跑出来的夏普主要在度量货币基金；换个行情它又几乎不筛。等于在回测里嵌了一层没人要求、页面上也没写的隐式择时，而且方向与「转债贵不贵」相反。

现在改为「比当期全市场中位便宜至少 5 个百分点」（`PDEStrategyConfig.min_relative_cheapness`，与批量页「低估候选」共用同一条判据和同一个常量），持仓上限仍由 Top N 承担。实测是纯增量：候选 3 → 39（另一份缓存 4 → 37），持仓 3 → 10，**原来的候选是新候选的子集，Top 10 只进不出**。这条闸和它反向的「贵得离谱不买」共用同一道守卫，池子太小、拿不到全市场中位时两条一起让开，不会退回成绝对阈值。

策略页的「选债限制」现在能看见、也能调真正在筛的那八条阈值。此前面板上只有四个写着「不限」的区间框，而模型溢价、相对偏差、便宜度下限、剩余年限、评级、余额和两个正股开关一条控件都没有、一条都没往配置里传，全走库里的默认值——实测它们联合把主池候选从 291 砍到 123（剔掉 54%），其中单是评级下限就砍掉 49 只。想做「放开评级看看策略在低评级债上成不成立」这种最基本的敏感性研究，在界面上做不到，而且不会有任何提示告诉你没做到。

八个控件开箱就填着默认值（不再是空白占位符），**留空 = 不设这道闸**，评级下拉多一个「不限」。实测在当前主池上：评级放开 39 → 49，便宜度下限留空 39 → 123，八条全放开 39 → 303。开箱默认与库默认逐只相同（两份缓存都是 39 / 37 只，Top 10 一致），也就是说不动控件的话行为和之前一样。

Wind 取正股股息率从「四个候选字段各发一次」改为一次请求取全部候选。失败时的负缓存只认「字段无效」这一种错误，而股息率最常见的失败是**字段存在但这只股没值、没权限或返回空串**——那几种一次都不进负缓存，于是每只股票、每期都把四个候选从头试一遍。桩上实测连叫三次：正常发 3 次请求，而上述三种情况各发 12 次。取值口径一个字节没动（12 种响应形态逐一比对，返回值全部相同），候选顺序也保持原样——那四个是三种不同的股息率口径，顺序就是「哪个更权威」的唯一记录。

策略回测取数明显变少。历史条款投影层每算一次条款都要再打一发「取收盘价」的请求，用的是「估值日往前 15 天」这么一个窄窗口，而且直接问最内层——于是它同时绕开了运行内缓存（那层按宽窗口存整段再切片）和跨运行磁盘缓存的复用（那层按精确区间做键，每个估值日都是一个新键，冷跑一发不少）。现在每只债只取一次覆盖整段回测的宽窗口，之后按估值日切片。

拿磁盘缓存里的真实转债日线做了逐日比对：470 只债 × 33 个估值日 = **15510 组，结果差异 0**，而取数发数从 15510 降到 470（**减少 97%**）。窗口之外的估值日（终点会夹到「昨天」，好让磁盘缓存肯落盘，而界面默认区间的最后一期可能就是今天）原路走窄窗口，值也一致。

单债回测页不设这个提示，那条路径行为逐字不变。

策略页「诊断」现在真的能看到块自助的统计稳健性了：Sharpe 的置信区间与为正概率、超额的置信区间与跑赢基准概率、滚动 Sharpe（1 年窗）的均值/最差/为正窗占比。引擎每次都算好放在结果里，但此前**只有命令行会打印**——界面上一个字都没有，导出的 CSV 里也没有，而使用文档的「结果怎么读四条铁律」第 ② 条早就写着"再看「诊断」：块自助给 Sharpe 置信区间与跑赢基准概率，CI 含 0 = 差异可能只是运气"。这不是新功能，是文档已经承诺、实现却缺席。

三段都带上样本量（block / 自助次数 / 配对期数 / 窗数）：置信区间的宽窄几乎全由期数决定，没有这个数就判不出「CI 含 0」是策略不行还是样本太短。算不出来时会说清是哪一种：旧快照缺这段数据（重跑一次就有）还是期数不够（重跑也没有，得拉长区间或调密调仓）——两种缺席要做的事正好相反。

CSV 导出新增 `# stability` 段。命令行、界面、CSV 三处共用同一份措辞。

策略页新增「股息率%」输入框。留空还是按数据源逐只取（和以前一样），填一个数就整段跳过那次取数——那是回测里最贵的一步：逐只债、逐期联网，而跨运行磁盘缓存偏偏不缓存它，拉的还是实时快照拿去给历史估值日用。实测一次 3 期回测在这一步卡了 55 分钟没有进度，623 只债全部取失败后回落 0，也就是和直接填 0 同样的结果，只是花了 55 分钟才知道。命令行早就有 `--q`，界面上一直没有。

回测运行中的行情缓存不再把「取数失败返回的空序列」当成权威结果记住。彻底失败和「这个窗口本来就没有行情」在数据源那一层长得一模一样，而东财按出口 IP 限流是常态、熔断冷却默认 5 分钟——一次封禁就足以让被碰到的债从整段回测的候选池和基准里一起消失，回测照常跑完、照常给出夏普，同一份配置跑两遍结果不同却没有任何提示。跨运行的磁盘缓存早就有这道闸，运行中的这一层漏了，而它才是每一期都要问一遍的那层。诊断里新增「空结果重取」计数。

「HV%」上限的占位符从「不限」改成「80」——那一格留空读的一直是「沿用默认 80%」，占位符写「不限」是页面上一句实打实的假话；行为没动，只是把它说对了。

命令行新增 `--min-relative-cheapness`（百分数，留空沿用默认 5，传负数关闭），`--max-deviation` 的帮助也补上了「默认不限」并指向前者；这条闸默认开着，所以关掉它得有明确入口。GUI 侧的控件随后续「把主口径阈值接进设置面板」一并补。

旧快照仍可回放（配置里一直记着 `max_deviation`，新增的 `min_relative_cheapness` 也进了快照）；如果你有不带 `--max-deviation` 的历史脚本，它们的结果会与之前不同。想完全恢复旧行为：`--max-deviation 0 --min-relative-cheapness -1`。

接口设置中的三个检测按钮合并为「检测并连接」：有路径就检测该路径，留空才自动查找。编辑路径或重新检测会清除旧结果；结果区分查找、加载和连接，加载失败时也保留已找到的文件位置。Windows 发布检查分别验证明确路径、从 Wind 安装根查找、从 Python 配置记录查找三条路径。

## 2.0.0rc6 — 2026-09-07

tag：`v2.0.0-rc.6`。桌面端可在应用内配置 Wind 接口，无需设置系统环境变量。

新增「同步池 → Wind 接口设置」：支持自动检测、选择本机 `WindPy.py`、测试终端连接及保存配置，Wind 错误窗口也可直接打开设置。检测在独立进程中运行，支持超时和关窗取消；接口可加载但终端未登录时仍可保存路径。设置保存在用户目录，重启后生效，当前任务及同步子进程继续使用原选择。环境变量覆盖优先于应用设置。

Windows 外置接口同时准备配套 DLL 搜索目录，不修改 Wind 安装文件；包内临时路径不会写入用户设置。GUI 启动只查找接口文件，不首次导入原生接口或连接终端，首次 Wind 取数由用户主动发起。

Windows 发布检查增加无控制台 EXE 加载外部接口替身的离线验收，验证检测入口、路径选择和正常退出；真实 Wind 运行库与终端连接仍需在安装了 Wind 的机器上测试。

## 2.0.0rc5 — 2026-09-07

tag：`v2.0.0-rc.5`。Wind 接口错误区分未找到接口、加载失败和连接失败，按平台提供处理指引；回测及定价取数弹窗默认折叠技术信息并支持复制，状态栏只显示简短原因。移除错误的 pip 安装建议，改为引导通过 Wind 终端安装或修复 Python 接口；补充 Windows 的 Wind `x64` 接口查找路径。

错误窗口不锁定主界面，回测失败后恢复操作按钮；后台只向界面传递文字，避免异常资源跨线程清理。保留 RC3/RC4 的新债扫描、UTF-8 管道及无控制台启动修复。

## 2.0.0rc4 — 2026-09-07

tag：`v2.0.0-rc.4`。补齐 Windows 池同步的编码修复：四个同步 CLI 统一输出 UTF-8，GUI 同步窗口按 UTF-8 读取，避免中文和 emoji 在 CP936 管道中导致同步中断。源码与冻结包的 CLI 入口均覆盖。

扫描失败后的 Wind 增量同步提示默认选择本地继续，并说明待更新债券较多时可能耗时十几分钟或更久；同步窗口可终止任务。Windows 发布检查增加脱离控制台、无标准句柄的冻结 EXE 离线测试，验证输出流补齐和真实 tqdm 路径后才允许上传安装包。

## 2.0.0rc3 — 2026-09-07

tag：`v2.0.0-rc.3`。修复 Windows 点击「扫新债」后整个窗口无响应的问题：无控制台启动时为依赖库补齐缺失的输出流，扫描及关注池定价的错误回调只传文字，避免进度条随异常进入界面线程并在析构时死锁。

新债清单改为直接读取同一公开数据接口，增加连接/读取超时、有限重试及分页完整性检查；失败时保留原条款库。候选版仍需实际 Windows 环境验收扫描、后续定价和重启恢复。

## 2.0.0rc2 — 2026-09-07

tag：`v2.0.0-rc.2`。修复 RC1 在 Windows 自动测试中暴露的并发文件替换与中文编码问题，并将 Windows Python 3.11 加入日常 CI。候选版仍需在实际环境验收 GUI、在线行情刷新和重启恢复。

## 2.0.0rc1 — 2026-09-07

tag：`v2.0.0-rc.1`。macOS 包已发布；Windows 自动测试未通过，未生成 Windows 发布包。Windows 试用请使用后续候选版。

相较于 `v1.0.0`，CBLens 从单债定价与批量筛选扩展为包含关注池、历史研究和桌面交付的可转债研究工作台。

### 主要变化

- **关注池成为默认主页**：保留上次落盘的理论价与估值日，支持今日刷新、新债上市日窄同步、事件日程和按日行情留档。
- **策略回测完整工作流**：按估值偏差选债，计入调仓、成交缺口、交易成本和现金收益，提供基准、归因、统计诊断与快照对比。
- **历史条款与数据修复**：按估值日投影转股价和事件状态，补充退市样本与条款变更记录，提供数据体检及公告存量修复命令；单债回测默认采用历史投影。
- **相对估值体系**：低估候选锚定当期全市场中位偏差，并限制候选规模；新增全市场估值历史分位。机会分已退役，质量分继续用于信用质量描述。
- **定价口径统一**：Provider API 的背景下修强度默认统一为 0.25/年，完善已提议下修节点、回售窗口、强赎截断与评级利差下限，修正转股价公告解析。
- **桌面交付与数据持久化**：支持 macOS / Windows 包、用户数据目录和内置历史条款 patch；策略快照也遵循用户数据目录与 `CBLENS_DATA_DIR`。发布构建与 tag、commit 绑定。

### 升级注意

本版包含破坏性变化：分发包名改为 `cblens`；部分 Python 参数与 CLI 开关删除；定价器的位置参数排列变化；批量 CSV 删除 `opportunity_score` 并增加多列。旧结果可以留档，但应使用新版本和明确参数重新计算，不能混作同一口径。

从 v1 或期间的开发版升级，请完整阅读 [升级指南](docs/UPGRADING_V2.md)。开发期的下修优势信号已删除；加载其旧快照与按新规则重新运行是两件不同的事。

### 候选版数据修复（2026-09-07）

- 对照 315 份公告原文复核 317 条巨潮转股价 patch，修正其中 119 条的价格或生效日；再次运行修复命令为 0 条待修改。13 条不能确定价格变更的记录和 2 条生效日早于公告日的追溯报告保留原值。
- 同步九强、汇通、希望转2、统联 4 只已生效的当前转股价，保留其他条款及 Wind 原取数时间。龙海等尚未生效的调整仍按生效日投影。
- 桌面种子更新至真实的 2026-09-03 行情快照（312 行），并使用原行情入参重算当日已生效的九强、汇通、希望转2条款；希望转2也纳入当日不下修公告。快照仍显示原行情日期，安装后需主动刷新。

### 已知边界

- 下修价下限按初始股价冻结，会让部分标的 Γ 为负或对网格步长敏感；本版不把 Gamma 收敛视为已解决的问题。
- akshare 的股息率兜底可能是实时快照，且股息率没有回测磁盘缓存；历史结果可能包含时点偏差，取数也可能较慢。显式固定 `q` 只能提供可重复的假设，不能替代历史数据。
- 强赎与下修触发仍含单点近似；回售期内给底比真实路径条件宽。历史条款和公告覆盖不足时仍会回退，必须结合来源诊断阅读结果。
- 桌面包暂未签名；Wind/akshare 实际可用性取决于本机与服务端。候选版本需完成实际启动、刷新、快照保存与重新打开验证后，才能作为正式版发布。

CBLens 用于研究与复盘。模型偏差、低估候选和回测超额均不构成已经验证的交易收益承诺。

## 1.0.0 — 2026-05-10

首次公开版本：Crank-Nicolson PDE 定价、多数据源、条款和公告缓存、批量筛选、GUI / CLI 与 Python API。
